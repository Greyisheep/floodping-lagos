# Copy to .env (gitignored) and fill in. NEVER commit the real key.
# AI Studio key (same mode as Dockie: GOOGLE_GENAI_USE_VERTEXAI=false).
GOOGLE_API_KEY=your-aistudio-key-here
GOOGLE_GENAI_USE_VERTEXAI=false
