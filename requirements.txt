# Pinned to match Dockie's validated ADK 2.2 stack — see issue #1.
# [db] pulls SQLAlchemy so DatabaseSessionService works (stateless sessions, issue #4).
google-adk[db]==2.2.0
asyncpg>=0.30          # async Postgres driver for Cloud SQL sessions (issue #6)
opentelemetry-sdk>=1.41               # OTel -> Cloud Trace (issue #7)
opentelemetry-exporter-gcp-trace>=1.9
google-genai>=2.4,<3
fastapi>=0.133,<1
uvicorn>=0.35
pydantic>=2.11
